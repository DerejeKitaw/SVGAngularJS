import { SvgDropTargetDirective } from './svg-drop-target.directive';

describe('SvgDropTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgDropTargetDirective();
    expect(directive).toBeTruthy();
  });
});
